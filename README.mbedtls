* CMake
  * `CMakeLists.txt`
    * Replace `-DCLIENT_USE_OPENSSL` with `-DCLIENT_USE_MBEDTLS`
  * `src/apps/common/CMakeLists.txt`
    * Add `find_package(MbedTLS)` and `list(APPEND COMMON_LIBS MbedTLS::tfpsacrypto)`
    * **This hasn't been tested yet**

## TODO

* Better configure support
* Better CMake support
